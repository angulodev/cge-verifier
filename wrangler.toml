name = "cge-verifier"
main = "worker/index.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Assets del frontend (build de Vite)
[assets]
directory = "./public"

[vars]
SITE_URL = "https://cge-verifier.angulodev.workers.dev"

# Secrets — wrangler secret put NOMBRE
# SUPABASE_URL
# SUPABASE_ANON_KEY
# MP_ACCESS_TOKEN
# WORKER_URL
